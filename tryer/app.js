/* let profiles = (tampil) => {
     console.log(`tampil dengan log ini ${tampil}`)
 }
 profiles('muantabs')*/

 let binatangs = ['ayam', 'sapi', 'kera', 'ayam']

 let result = binatangs.map(ekor => {
     return ekor + ' ntap soul'
 })
 console.log(result)